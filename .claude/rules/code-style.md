# Code style

- Follow existing naming, formatting, and component patterns.
- Group imports in this order, separated by blank lines:
  1. Built-in modules, for example `node:path`.
  2. Framework and third-party packages, for example `next`, `date-fns`, or `next-themes`.
  3. Project imports, for example `@/lib/utils` or `@/components/...`.
